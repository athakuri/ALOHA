@{
    ViewBag.Title = "Add";
}

<h2>Add</h2>
@using (Html.BeginForm(new {ReturnUrl = ViewBag.ReturnUrl}))
{
     @Html.ValidationSummary(true)
    
    <div>
        @Html.LabelFor(m => m.C_Name)
        @Html.TextBoxFor(m => m.C_Name)
        @Html.ValidationMessageFor(m => m.C_Name)

    </div>
    <input type="submit" value="Add" />
}